import {StyleSheet} from "react-native";

export const CommonStyles = StyleSheet.create({
    containerHome: {
        justifyContent: 'center',
        alignItems: 'center',
    },
    contained: {
        marginVertical: 20,
        display: 'flex',
        flexDirection: 'row',
    },
});
