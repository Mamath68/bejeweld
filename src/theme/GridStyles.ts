import {StyleSheet} from "react-native";

export const GridStyles = StyleSheet.create({
    image: {
        justifyContent: 'center',
    },
    containerGrid: {
        justifyContent: 'center',
        alignItems: 'center',
        padding: 10,
    },
});
