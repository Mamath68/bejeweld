import { StyleSheet } from "react-native";

export const GridStyles = StyleSheet.create({
    image: {
        justifyContent: 'center',
    },
    container: {
        justifyContent: 'center',
        alignItems: 'center',
        padding: 10,
    },
});