import { StyleSheet } from "react-native";

export const CommonStyles = StyleSheet.create({
    container: {
        justifyContent: 'center',
        alignItems: 'center',
    },
    gameContainer: {
        marginVertical: 20,
        display: 'flex',
        flexDirection: 'row',
    },
});