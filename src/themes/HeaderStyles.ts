import { StyleSheet } from "react-native";

export const HeaderStyles = StyleSheet.create({
    container: {
        alignItems: 'center',
    },
    title: {
        color: '#fff',
        fontWeight: 'bold',
        fontSize: 30,
    },
});