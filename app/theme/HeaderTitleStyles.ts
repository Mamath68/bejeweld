import {StyleSheet} from "react-native";

export const HeaderTitleStyles = StyleSheet.create({
    titleContainer: {
        alignItems: 'center',
    },
    headerTitle: {
        color: '#fff',
        fontWeight: 'bold',
        fontSize: 30,
    },
});
